defmodule Operator.SwitchTest do
  use ExUnit.Case
  alias Lily.Error
  alias OkComputer.Switch
  import Switch

#  defswitches(
#    ~>: {
#      fn a, f -> f.(a) end,
#      fn a, f -> f.(a) end
#    }
#  )
end
